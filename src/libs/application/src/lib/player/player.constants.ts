/**
 * Player Timer Constants
 *
 * Shared constants for player timer functionality across services, helpers, and tests.
 */

/**
 * Default timer duration in milliseconds (3 minutes)
 * Used as fallback when song metadata is missing or invalid
 */
export const DEFAULT_TIMER_MS = 180000;
