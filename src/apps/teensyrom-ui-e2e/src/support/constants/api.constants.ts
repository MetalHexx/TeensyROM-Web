/**
 * E2E API Constants - Single source of truth for API endpoints
 * Note: All TeensyROM API routes now use /api/ prefix
 */

export const API_CONFIG = {
  BASE_URL: 'http://localhost:5168',
  API_PREFIX: '/api',
} as const;
